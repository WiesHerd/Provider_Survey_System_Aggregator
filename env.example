# Firebase Configuration
# Copy this file to .env.local and fill in your Firebase project credentials
# NEVER commit .env.local to git - it contains sensitive API keys

# Get these values from Firebase Console > Project Settings > General
REACT_APP_FIREBASE_API_KEY=your_api_key_here
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
REACT_APP_FIREBASE_APP_ID=your_app_id

# ============================================================================
# Storage Mode Configuration (HYBRID MODE)
# ============================================================================
# 
# The app uses a hybrid storage system that automatically selects the best
# storage backend based on availability:
#
# HYBRID MODE (Default - Recommended):
#   - If Firebase is configured and available → Uses Firebase (cloud storage)
#   - Otherwise → Uses IndexedDB (local browser storage)
#   - IndexedDB is always initialized for seamless fallback if Firebase fails
#
# To use hybrid mode (recommended):
#   - Leave REACT_APP_STORAGE_MODE unset, OR
#   - Don't include this variable in your .env.local file
#
# EXPLICIT MODES (Optional - Override hybrid detection):
#   - Set to "indexeddb" to force IndexedDB only (works on Vercel, no backend)
#   - Set to "firebase" to force Firebase (requires Firebase credentials above)
#
# Examples:
#   REACT_APP_STORAGE_MODE=indexeddb  # Force IndexedDB only
#   REACT_APP_STORAGE_MODE=firebase   # Force Firebase (must be configured)
#   # (no variable)                   # Hybrid mode (auto-detect)
#
# ============================================================================
# Optional: Storage mode override
# Uncomment and set to force a specific mode (see above for options)
# REACT_APP_STORAGE_MODE=indexeddb

# ============================================================================
# Authentication Configuration
# ============================================================================
# 
# Controls whether authentication is required for the application.
# 
# PRODUCTION (Default):
#   - Authentication is required when NODE_ENV=production
#   - Set REACT_APP_REQUIRE_AUTH=false to allow IndexedDB-only mode in production
# 
# DEVELOPMENT:
#   - Authentication is optional by default
#   - Set REACT_APP_REQUIRE_AUTH=true to require authentication in development
#
# Examples:
#   REACT_APP_REQUIRE_AUTH=true   # Require authentication (production default)
#   REACT_APP_REQUIRE_AUTH=false  # Allow IndexedDB-only mode (development default)
#   # (no variable)                # Production: require auth, Development: optional
